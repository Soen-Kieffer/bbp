from distutils.file_util import copy_file
import shutil


def DoMaj():
    print("la mise à jour va s'effectuer.")
    copy_file()
    source=""
    destination=""
    shutil.copyfile(source, destination)
